<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Your Recipe Book</title>
  </head>
  <body>
    <a href="/recipes/new">Click Here to Add a Recipe!</a>

    <% Recipe.all.each do |recipe|%>
    <div class="recipe">
        <h4><%=recipe.name%></h4>
        <p><%= recipe.ingredients %></p>
        <p><%= recipe.cook_time%></p>
        <a href="/recipes/<%= recipe.id %>">See this recipe</a>
        <a href="/recipes/<%= recipe.id %>/edit">Edit this recipe</a>
    </div>
    <%end%>
  </body>
</html>
